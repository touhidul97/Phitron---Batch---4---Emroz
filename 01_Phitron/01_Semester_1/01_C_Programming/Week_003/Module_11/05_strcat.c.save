#include <stdio.h>
#include <string.h>

int main() {
    char a[200];
    char b[100];
    
    scanf("%s", a); // Read input for a
    scanf("%s", b); // Read input for b
    
    strcat(a, b); // Concatenate b to the end of a

    printf("%s\n", a);//Concatenated string:

    return 0;
}
