#include<bits/stdc++.h>
using namespace std;
int fun(stringstream& ss)
{
    string word;
    int cnt=0;
    if(ss >> word)
    {
        cnt++;
        fun(ss);
    }
    retun cnt;
}
int main()
{
    string s;
    getline(cin,s);
    for(int i=0; i<s.size();i++)
    {
        if(s[i]=='!' || s[i] =='?' || s[i]==',')
        {
            s[i]=' ';
        }
    }
    stringstream ss(s);
    
    return 0;
}