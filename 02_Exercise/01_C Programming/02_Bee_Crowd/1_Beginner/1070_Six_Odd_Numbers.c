#include <stdio.h>
int main()
{
    int x;
    scanf("%d",&x);
    int current, counter;
    current = x;
    counter = 0;
    while (counter < 6)
    {
        if (current % 2 != 0)
        {   
            printf("%d",current);
            cu
        }
        counter++;
    }
    return 0;
}